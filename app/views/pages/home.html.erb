<div class="site-wrapper">
	<div class="site-wrapper-inner">

		<div class="inner cover">
            <h1 class="cover-heading">Need a spot?</h1>
            <p class="lead">Supporting SQL, Presto</p>
            <% if user_signed_in? %>
          		# do something
     		<% else %>
       			<p>
         			<%= link_to "Learn more", new_user_registration_path, class: "btn btn-default btn-lg" %>
         			<%= link_to "Sign up", new_user_registration_path, class: "btn btn-primary btn-lg" %>
       			</p>
       		<% end %>
      	</div>
	</div>
</div>